info:
  name: First 10 patients
  type: http
  seq: 3

http:
  method: GET
  url: "{{http_base_url}}/fhir/r4/Patient?_elements=id,name&_count=10"
  params:
    - name: _elements
      value: id,name
      type: query
    - name: _count
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get first 10 patients.
