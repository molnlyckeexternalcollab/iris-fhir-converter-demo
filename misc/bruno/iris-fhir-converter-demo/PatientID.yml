info:
  name: PatientID
  type: http
  seq: 8

http:
  method: GET
  url: "{{http_base_url}}/fhir/r4/Patient/4243/$everything"
  auth: inherit

runtime:
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: |-
          const total = res.getBody().total;
          console.log("Patient count:", total);
        method: jsonq
      variable:
        name: ""
        scope: runtime

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get number of patients are stored.
​