"""FhirClient protocol and implementations.

Sub-modules:
  fhir_client.base   — FhirClient protocol (structural typing)
  fhir_client.httpx  — HttpxFhirClient (default, uses httpx)
  fhir_client.iris   — IrisFhirClient (future: wraps IRIS native FHIR adapter)
"""
